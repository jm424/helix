\* Raft TLA+ Model Checking Configuration
\* Run with: java -jar tla2tools.jar -config raft.cfg raft.tla

\* Model values
CONSTANTS
    Servers = {s1, s2, s3}
    MaxTerm = 3
    MaxLogLength = 4
    Nil = "nil"

\* Specification
SPECIFICATION Spec

\* Disable deadlock checking - deadlock is expected when MaxTerm is reached
\* or when no actions are enabled (finite model)
CHECK_DEADLOCK
    FALSE

\* Safety properties to check
INVARIANTS
    TypeOK
    SingleLeaderPerTerm
    LogMatching
    StateMachineSafety
